int main()
{
	pid_t pid;

	pid = fork();
	if (pid == 0)
	{
		fork();
	thread_create()
	}
	fork();

	// How many unique processes are created?
	// 6
	// How many unique threads are created?
	// 8 (2)
}
